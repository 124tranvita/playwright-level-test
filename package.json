{
  "name": "playwright-level-test",
  "version": "1.0.0",
  "description": "This repository contains the automated test suite for the Agoda Flight Search functionality, built using **Playwright** and **TypeScript**. The project is designed to demonstrate professional automation practices, focusing on maintainability, scalability, and performance.",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test --workers=1",
    "headed": "npx playwright test --headed --workers=1",
    "ui": "npx playwright test --ui --workers=1",
    "debug": "npx playwright test --debug --workers=1",
    "report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.2.3",
    "allure-playwright": "^3.4.5"
  }
}
